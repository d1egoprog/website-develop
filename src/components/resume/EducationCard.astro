---
const { education } = Astro.props;
---

<tr>
    <td> {education.data.dates.completed.substring(0,4)} </td>
    <td>
        <strong> {education.data.degree.level} (
        {education.data.degree.abbreviation})</strong> in <a href={education.data.degree.url} target="_blank">
            {education.data.degree.name}
        </a> at the <a href={education.data.department.url} target="_blank"> 
            {education.data.department.name}
        </a> from the <a href={education.data.institution.url} target="_blank">
            {education.data.institution.name}
        </a>
    </td>
    <td>    
        {education.data.location.city}, {education.data.location.country}
    </td>
</tr>

<!-- _
<article class="education-card">
    <div class="year">
        {education.data.dates.completed.substring(0,4)}
    </div>
    <div class="content">
        <h3>{education.data.title}</h3>
        <h4>{education.data.institution.name}</h4>
        {
            education.data.department &&
            <p>{education.data.department.name}</p>
        }
        {
            education.data.research &&
            <h5>Research Topic</h5>
            <p>{education.data.research.title}</p>
        }{
            education.data.thesis &&
            <h5>Thesis</h5>
            <p>{education.data.thesis.title}</p>
        }{
            education.data.degreeProject &&
            <h5>Degree Project</h5>
            <p>{education.data.degreeProject.title}</p>
        }    
    </div>
    -->

</article>